---
title: Frequently asked questions
description: Common questions about running and interpreting BenchKit.
---

## Is this a Laravel package I install?

No. BenchKit is a standalone app you spin up, benchmark with, and then throw away. It never goes near your own codebase.

## Do I need Docker?

No. Clone the repo and run it anywhere Laravel runs, including Laravel Cloud. The benchmark tools arrive with `composer install`. See [Running from source](/docs/getting-started/running-from-source).

## Can I compare two runs?

Yes. Run it once on each setup and open them side by side to see exactly what your change did.

## Do my results survive a rebuild?

Mount a volume at `storage/app/runs` and they do. Without one, your history lasts as long as the container.

## Does closing the tab stop my run?

No. The run belongs to the server rather than to your browser. You can close the tab, reload the page, or open it on your phone and still be watching the same run. Any tab can stop it, and none of them can start a second one.

## Can I run two benchmarks at once?

No, and that is deliberate. Two benchmarks competing for the same CPU would each measure the other. BenchKit holds the claim against a live process. If a run is somehow abandoned, the next one starts rather than waiting forever.

## How do I benchmark FrankenPHP worker mode?

Pull the FrankenPHP image and start it through Laravel Octane. The same image also runs in classic mode, which is what makes the comparison a fair one. See [FrankenPHP](/docs/image-variations/frankenphp).

## Can I benchmark against MySQL or Postgres?

Yes. Set the standard Laravel `DB_*` variables and BenchKit creates the tables it needs on its own. See [Adding a real database](/docs/configuration/adding-databases).

## Why does BenchKit say some of my cores were idle?

Under PHP-FPM one request occupies one worker. A pool smaller than your core count leaves cores with nothing to do, and the result understates the machine. Raise `PHP_FPM_PM_MAX_CHILDREN` to at least the core count and run again. See [Reading your results](/docs/benchmarks/reading-your-results).

## Can I run the load test from my own machine?

Yes. The four load test routes are open endpoints, so you can point oha at them from your laptop or another server. Press **Prefer your own tools?** on the start screen and BenchKit hands you the commands, already pointed at your server. See [Testing from your own machine](/docs/benchmarks/testing-from-your-own-machine).

## How accurate are these numbers?

They are most accurate when you compare two runs on the same machine, which is what BenchKit is built for. The limits that apply, and what to do about each, are covered in [Reading your results](/docs/benchmarks/reading-your-results).

## Where do I get help?

- [Discord](https://serversideup.net/discord) for community support
- [GitHub](https://github.com/serversideup/benchkit-laravel) for source, issues, and discussions
- [Professional help](https://serversideup.net/professional-support) for direct support from the team
