.vault-password
.github
.infrastructure
!.infrastructure/**/local-ca.pem
!.infrastructure/**/local-dev-key.pem
!.infrastructure/**/local-dev.pem
!.infrastructure/entrypoint.d/
Dockerfile
docker-*.yml
.gitlab-ci.yml
.spin*
scripts/
# The marketing/docs site is a standalone Nuxt project deployed to Cloudflare
# Pages — it must never ship inside the BenchKit image (or bloat the node
# build context). The final and node-build stages COPY the whole context.
docs/